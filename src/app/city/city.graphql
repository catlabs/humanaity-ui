query GetCities {
  cities {
    id
    name
  }
}

query GetCity($id: ID!) {
  city(id: $id) {
    id
    name
  }
}

subscription PositionSub($cityId: ID!){
  humansByCityPositions(cityId: $cityId) {
    id
    name
    x
    y
    happiness
  }
}
